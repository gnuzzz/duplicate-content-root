group 'ru.albemuth.samples.dcr'

apply plugin: 'base'

defaultTasks 'all'

subprojects {
  apply plugin: 'java'

  sourceCompatibility = "1.8"
  targetCompatibility = "1.8"
  tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
    options.compilerArgs += ['-parameters']
  }
}

